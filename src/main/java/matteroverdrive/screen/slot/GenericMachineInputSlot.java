package matteroverdrive.screen.slot;

import net.minecraft.inventory.Inventory;
import net.minecraft.screen.slot.Slot;

public class GenericMachineInputSlot extends Slot {
    public GenericMachineInputSlot(Inventory inventory, int index, int x, int y) {
        super(inventory, index, x, y);
    }
}
